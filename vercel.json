{
  "buildCommand": "npm install && npm run dev",
  "outputDirectory": "dist",  // Optional if needed
  "cleanUrls": true,
  "functions": {
    "api/**/*.ts": {
      "runtime": "vercel-node@1.0.0"
    }
  }
}
